# Description

A VBscript file `print_movie_list.vbs` that reads meta information from .MP4 files and writes it into a .CSV file. Accompaning Python script `csv_to_JSON.py` converts the resulting CSV data into JSON data.
